{
    "required": true,
    "package": "${modGroup}.${modid}",
    "compatibilityLevel": "JAVA_${javaVersion}",
    "maxShiftBy": 0,
    "mixins": [
${mixins}
    ],
    "client": [
${clientMixins}
    ],
    "minVersion": "0.8"
}